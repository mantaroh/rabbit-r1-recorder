# 設計（実装済みの姿）

現在のシステムが何をしているか、そして当初の仕様書からどこが変わったか。
以下の相違はすべて、端末またはデータ上の実測によって強制されたものである。
判断の根拠となった数値を残してあるので、事実が変われば議論し直せる。

English: [`DESIGN.md`](DESIGN.md)

## これは何か

**周囲の音声を録り、残し続ける Rabbit R1。**

成果物は録音そのものである。それ以外——文字起こし、検索、質問機能——は
「今のモデルにたまたまできること」であって、すべて後から同じ音声に対して
やり直せる。音声だけはやり直せない。この非対称性が、本書のほぼすべての判断を
決めている。

その上に載っている機能として、サイドボタンを2回押して質問すると、直前2分間を
文脈として渡された自前ホストの Hermes エージェントが答える。

## 全体像

```
  サイドボタン ──▶ KeyService (アクセシビリティ)
                       │ 2回押し
  加速度センサー ▶ Motion ─┬─ 姿勢          │
                           └─ シェイク ▶ カメラ / チャット
                       ▼
  マイク ──────▶ RecorderService ──▶ RingBuffer (150秒・メモリ)
   48 kHz ステレオ     │  │                │
                       │  ├─ 60秒 Opus セグメント ┐
                       │  │  + RMS エンベロープ   │ 質問時: 発話 + 直前2分
                       │  ├─ Timelapse ──▶ jpeg ──┤
                       │  └─ Positions ──▶ 測位   │
                       ▼                          ▼
                 QueryController ──▶ PUT /v1/segments/{id}, /v1/photos/{id}
                                     POST /v1/positions
                                             │
                                    Cloudflare Access
                                             ▼
                                     Worker ──▶ R2 (音声・写真・保持)
                                             ├─▶ Whisper ──▶ D1 (文字起こし)
                                             ├─▶ 画像モデル ▶ D1 (キャプション)
                                             └──────────────▶ D1 (測位)
                                                              │
  ChatActivity ◀── 文字起こし  ブラウザ ▶ GET / , /v1/map/* ──┤
       │                                 タイムライン・地図   │
       └──▶ Hermes gateway ──▶ agent ──MCP: lifelog_recent────┘
```

文脈はエージェントが自分で取りに行く。文字起こしと文脈ブロブを一緒に運ぶ専用の
質問 API は存在しない。R1 は普通のプロンプトを投げるだけで、`lifelog_recent`
を呼ぶかどうかはエージェントが決める。これで当初設計からエンドポイントが1本
まるごと消え、「さっきの話」が特別扱いなしに機能するようになった。

## 録音形式

48 kHz ステレオで取り込み、Opus 32 kbps/ch で保存する。

**サンプルレートだけは後から変更できない。** 8 kHz で帯域制限された録音は、
将来どれだけ優れたモデルが読もうと帯域制限されたままである。このシステムは
最初の数週間を 16 kHz で動かしており、その期間はすべて恒久的な損失になった。

マイクは最初から制約ではなかった。端末のオーディオポリシーは `Built-In Mic`
に対して `8000 16000 32000 44100 48000` を提示しており、48 kHz で実測した
スペクトルには旧ナイキスト周波数の位置に崖がない。

| 帯域 | RMS | | 帯域 | RMS |
| --- | --- | --- | --- | --- |
| 300 Hz – 1 kHz | −55.8 dB | | 8.5 – 10 kHz | −71.7 dB |
| 2 – 4 kHz | −64.6 dB | | 11 – 13 kHz | −70.4 dB |
| 5 – 7 kHz | −68.1 dB | | 15 – 17 kHz | −75.5 dB |
| | | | 18 – 20 kHz | −80.0 dB |

16 kHz をアップサンプルした信号なら 8 kHz 以上で急落する。これは急落せず、
11–13 kHz は 8.5–10 kHz より大きい。実信号である。

保存形式は逆に、後から変更できる種類の決定であり、要は金の問題である。Opus は
非可逆だが、48 kHz ならマイクが出す全帯域をごく一部の容量で保持する。
**ステレオで1分あたり 550 KB、連続録音で年間およそ 280 GB。** 以前のように
16 kHz PCM を可逆で保存するのは、より多くのバイト数を払って部屋の情報をより
少なく残す行為だった。

2チャンネルとも保存している。その理由は当初想定したものではない
（[方向](#方向がとれない理由)を参照）。ただし2つは実際に別物で、Opus を通した後でも
差信号は各チャンネルの 15 dB 下にある——ノイズフロアではない。符号化がステレオを
潰してデュアルモノにしているわけではない。

形式はセグメントを閉じた時点でサイドカーに書き、音声と一緒に運ぶ。以前は
アップローダーが**送信時点の**レコーダー設定を報告しており、設定変更を跨いで
キューに残っていたファイルに誤ったラベルが付いた。実際、モノラルのセグメントが
ステレオとして保存された。**誤って記述された録音は、永久に誤って復号される。**

## 保持期間

**無期限。** `audio/` へのライフサイクルルールも、cron も、スイープもない。

以前の版は音声も文字起こしも1日で消していた。これは文字起こしをシステムの目的だと
取り違えていた時期の設計で、再生成できない唯一のものを、それを保管する費用を
惜しんで削除していた。撤去の際、既に索引を失っていた録音327件を回収した。
`RETENTION_SECONDS` があった場所には、現在その経緯を書いた注記が置いてある
（[`cloudflare/lifelog/src/index.ts`](cloudflare/lifelog/src/index.ts)）。

R2 は $0.015/GB月。消さないので単調に増える。

| | 1年目 | 5年目 | 5年総額 |
| --- | --- | --- | --- |
| Opus 48 kHz ステレオ・年280 GB | 約$4/月 | 約$19/月 | 約$570 |
| 写真・年数 GB | 無視できる | | |
| 地図アーカイブ・1.48 GB（一度きり） | 年$0.27・横ばい | | |
| 文字起こし（1日の約11%に限定） | 約$3/月 | | |
| キャプション（1枚 約$0.00026） | $1/月未満 | | |

いまや保管費より**文字起こし費のほうが大きい**。そしてこちらは、恒久的な損失を
伴わずに削れる唯一のコストである。音声は残るので、いつでも再度文字起こしできる。

## VAD は何のためにあるか

**文字起こしを絞るためであって、保存を絞るためではない。** 録音は全部送って
全部残し、Whisper を呼ぶかどうかだけをゲートする。

無音を Whisper に渡すと幻覚が出る。`Thank you.` や「ご視聴ありがとうございました」
を、でっち上げたという印もなく返してくる。モデル自身の `vad_filter`（現在有効）は
その大半を抑えるが、それは呼び出して課金された後の話であり、**録音のうち発話は
約11%**——勤務時間帯で4–20%、帰宅後の自宅で10–53%、就寝中はほぼゼロ——にすぎない。

**どこで測るかはコーデックに強制されている。** ここでの Opus は実質 CBR で、
無音セグメントの平均 271,859 バイトに対し発話ありが 293,769 バイト、範囲は完全に
重複する。パケットからは何も読み取れない。Worker で波形を見るには WASM の Opus
デコーダが要る。一方、端末には PCM があり、そもそも毎フレーム RMS を計算している。

そこで端末は**1秒あたり1バイトの音量**を base64 で添えて送る（1分60バイト、
年間約500 KB）。しきい値を当てるのは Worker。**端末は測定値を報告し、
ポリシーは Worker が持つ。**

| | RMS ÷ 16 |
| --- | --- |
| 静かな部屋（1分間の実測） | 6 – 16 |
| しきい値 | **32** |
| 発話 | 152 – 221 |

60秒のうちしきい値を超える秒が2秒未満のときだけスキップする。この非対称は意図的で、
誤ってスキップすれば誰かが気づくまで文字起こしを失うが、誤って文字起こしすれば
$0.0005 で済む。

そしてこれは一切不可逆ではない。エンベロープを保存してあるので、
`POST /v1/admin/rejudge` が別のしきい値を過去に再適用でき（`?dry=1` で影響を確認）、
新たに有声と判定されたものを再投入する。**音声を1バイトも読まずに。**
判定結果ではなく測定値のほうを保存している理由がこれである。

## 写真

1サイクルにつき前後1枚ずつ、640 px、1枚あたり約 60 KB。

レンズはモーターアーム上のセンサー1個なので、「前後」は物理的な回転を意味する。
そしてそのアームは音を出す——音声を記録するための装置の上で。ここから4つの規則が出る。

**間隔は端末がどこにいるかで変わる。**

| | 間隔 |
| --- | --- |
| 自宅 Wi-Fi | 15分 |
| それ以外 | 5分 |
| 自宅かつ静かかつ暗い | 停止 |

自宅は「同じ部屋・同じ家具」が延々と続く、最も新しい絵が出にくい場所であり、
外はその逆で、最も密度が高く二度と撮れない。判定は SSID から読む。そしてこの
判定は、変化したときに必ずメトリクスへ書くようにした。**誤っても失敗しないから
である**——シャッター間隔が黙って3倍になるだけで、故障ではなく「よく撮る
タイムラプス」に見える。実際に48分間そうなっていた。

**発話中はサイクルを最大2分まで延期する。** 会話の上にモーター音を重ねないため。
撮影はその直後の静かな瞬間に行う。

**何も起きていないならサイクルごと省略する。** 誰も入ってこない部屋は前回と同じ
2枚を生むだけで、テレビがついた部屋は「無音ではないが同じ」を生む。そこで部屋が
普段どのくらいの音量かを baseline（約10分の緩やかな平均）として持ち、前回以降に
**発話があった**か **baseline を明確に上回るピークがあった**ときだけ撮る。
テレビをつけっぱなしにすると baseline がピークと一緒に上がるので条件を満たさなくなる。
発話は常に条件を満たす。停止条件は論理積である——静か**かつ**暗い**かつ**自宅。
外出先の暗い部屋は撮る。

**台座にあるときは画面側しか撮らない。** 台座に座っている状態では、後方カメラが
見ているのは壁であり、15分後も同じ壁である。省けばサイクルは半分になり、音が
聞こえるアームを1往復ぶん振らずに済む。

台座は角度で判定する。そして角度は紛れようがない:

| 静置状態 | 実測 | 傾き |
| --- | --- | --- |
| 机に直立 | (0.08, 9.60, −0.07) | 0° |
| 机に直立 | (0.06, 9.58, 0.61) | 3.6° |
| **印刷した台座** | **(0.04, 8.75, 4.03)** | **24.7°** |

台座は dylanfonz 氏の [Rabbit r1 Stand](https://makerworld.com/en/models/406818-rabbit-r1-stand)
を 3D プリンタで出力したもの。**24.7° はこのモデル固有の値であって、台座一般の
値ではない。** 別の設計や、ケースに入れたまま座らせた場合は違う値になる。撮影が
黙って1サイクル2枚に戻ったら、最初に確認すべきはこの帯域である。

**20度の開き**があり、しかも台座の値はサンプル間で 0.01 g 単位まで安定している。
水平でない机がこの差を越えることはない。だから帯域は 12〜45° とし、両側に約8度の
余裕を取った。上限も恣意的ではない——約47度を超えると重力が Y より Z に多く乗り、
そもそも「直立」と分類されなくなる。

これは当初「立てたまま・動かず・給電中」と書いていた。台座の傾きが机と区別できる
はずがないという前提だったが、そのとき**台座はまだ出力されていなかった**。実物が
できた今、**直接の計測は代理指標に両方向で勝る**: ケーブルは机を台座に変えないし、
満充電——充電中と報告されなくなる——は端末を台座から降ろさない。

したがって机の上に直立させた端末は、いまも両方向を撮る。それが間違える側として
安全な方向である。壁の冗長な1枚はシャッター1回の損だが、部屋の1枚を逃せばそれは
戻らない。

**落とす側が前方になることはない。** 前方フレームには部屋と人が写っているし、
暗さの測定もそこから取っている。省けば上の「就寝中」規則が黙って無効になる。

ここで読む1秒ごとの音量は、VAD エンベロープが既に計算しているものと同じである。

**各フレームは到着時に画像モデルが1回だけキャプションを付ける。** 1枚あたり
約 $0.00026、音声に比べれば端数である。そしてこれが写真を検索可能にする唯一の
手段でもある——JPEG には他に手がかりがない。キャプションは索引であって記録では
ない。フィルタもかかるし作り直せる。その裏にある写真だけが単体で成立する。

回転角はカメラごとに異なり、純正カメラアプリに合わせて較正した値
（フロント180°、リア0°）を使う。「センサーはセンサーだろう」という理屈で一度
両方0°に上書きし、自撮りが全部逆さまになった。元の値が正しかった。

## 位置

自宅で15分ごと、外で5分ごと。タイムラプスと同じ規則だが、理由は違う。

**この端末にはネットワーク測位が存在しない。** `dumpsys location` が並べるのは
passive・fused・gps だけである。CarrotOS には Play Services が入っておらず、
AOSP の fused プロバイダは GPS の薄いラッパー、passive は他が要求した結果を
繰り返すだけ。つまり**屋内ではどれだけ待っても測位できない**し、待つ分だけ
受信機が電気を食う。自宅の位置は既知でありかつ取得不能なのだから、5分ごとに
失敗しに行っても得るものがない。

**測位失敗も書き残す。** そうしないとトラックの穴が「端末が止まっていた」と
読めてしまう。「屋内にいた」とは別の事実であり、追いかける価値があるのは
片方だけである。

位置は音声や写真と違い、耐久性の契約が変わる。**R2 に実体がなく、行そのものが
成果物**なので、失われたバッチは単に失われる。だからバッチ POST にし、
`(device_id, recorded_epoch)` で冪等にして端末が自由に再送でき、ローカルの
キューは truncate ではなく rename で受け渡す——読み出しと書き込みの隙間に、
アップロード中に取れた fix が落ちないように。

緯度経度は解析するだけでなく範囲検査もする。**地球上にありえない行は、欠損行
より悪い。** 地図に空いた穴が「移動」に見えるからである。

**精度は保存し、それで濾さない。** 2 km の fix と 5 m の fix を同じように描けば、
道を歩いた記録が「隣の建物の中を歩いた記録」になる。だから数値を渡して描画側に
決めさせる。悪い fix を薄く描くことはできるが、入口で捨てた fix を後から
発明することはできない。

## 姿勢と、シェイク

加速度センサー1本、導出は2つ。**低周波が「どちらが下か」、高周波が
「どれだけ強く動いているか」**を語る。1秒ごとの RMS が発話ゲートとタイムラプス
両方に効いているのと同じ構えで、1つの運動についての2つの問いにセンサーを
2回回す理由はない。

プラットフォームの `GRAVITY` と `LINEAR_ACCELERATION` ではなく生の加速度を使う。
一見こちらが自然だが、この端末ではどちらも AOSP の合成センサーで、要求すると
2行のフィルタで足りるもののためにジャイロまで合成に引き込まれうる。加速度計は
`minRate=50Hz` も報告するので、そもそも遅い設定は選べない——登録した時点で
50 Hz である。

姿勢は3秒デバウンスし、動きの方はイベントにしない。持ち上げる途中で2〜3の姿勢を
通過するし、`moving` はポケットの中で数秒ごとに反転するので、メトリクスログが
歩数計になる。

**シェイクはピーク数で数え、対応は実測した。** 「2回振った」ときに手首が何ピークを
出すかは、手首についての事実である。

| 意図 | 実測ピーク数 |
| --- | --- |
| 「2回」 | 2, 2, 2, 2, 2 |
| 「3回」 | 3, 4, 4, 5, 4 |

分布が重ならないので、境界の置き場所は1つしかない。**2ピークでカメラ、3以上で
チャット。** 置き換える前の推測は「回数＝ピーク数」を仮定していたが、それは違う
——手首を3回振れば方向反転は4回起きる——ため、その日の最初のジェスチャが
反対側に落ちていた。

しきい値は 14 m/s²。意図的なジェスチャ群の後、次まで47分間、持ち上げ・設置・
裏返しをしても誤検出はゼロだった。ただしこれは**証拠であって結論ではない**。
端末は机の上にあり、シェイク検知が普通に失敗するのは歩行中である。

## 地図

完全に自己ホストしている。アーカイブ、スタイル、レンダラ、フォント、アイコン、
すべて R2 から Worker 経由で出るので、**ページは third party に一度も要求を
出さない。**

直接の理由は、PMTiles の配布元がまさにそれを求めているから——
「本サイトのPMTiles URLをアプリから直接参照する利用はご遠慮ください」——
ダウンロードして自分で置くよう案内されている。他の理由は元から成立していた:
API キー不要、リクエスト課金なし、個人のログの経路に third party がいない、
R2 は egress 無料。1.48 GB で**年 $0.27** である。

**PMTiles は1ファイルをバイトオフセットで読む形式**なので、配信するとは Range
要求に答えることを意味する——これはこの Worker が既にやっていた。1分の音声で
シークバーを掴んだときのために。同じ仕組み、違うバイト列である。スタイルが
配布元を指している唯一の参照は取り込み時に書き換える。そうしないと、まさに
それを避けるためにミラーしたものを、地図表示のたびに直参照することになる。

フォントだけが非対称である。このスタイルはフォントスタック6種、各256レンジ。
全部ミラーすれば1536ファイルで、そのほとんどはこの地図が決して描かない文字体系
であり、それを個人アーカイブのためにボランティア運営のサーバーへ一気に取りに行く
ことになる。そこで**初回利用時に取り込む方式**にした。1画面が要求するのは十数個、
それも一度きりで、以後はローカルにある。CJK レンジの初回が保存込みで 812 ms、
同じレンジの2回目が 282 ms。

測位点は精度に応じた大きさで描き、線でつなぐ。線は点に順序を与えるだけのもので
ある。5分離れた2つの目撃点を結ぶ直線は、その間の経路についての主張ではないし、
そう読まれないように点自体を消していない。

## 方向がとれない理由

ステレオ収録は本来、**話している人の方向にレンズを向けるため**に導入した。それは
できない。二度と試さずに済むよう、理由を記録しておく。

**音量差では取れない。** 前から話した場合と後ろから話した場合の差は、広帯域で
0.2 dB、4 kHz 以上でも 0.3 dB、分布も重なる。障害物より波長が長い音は回り込む。
音声の 100 Hz–4 kHz は波長 8.6 cm–3.4 m、対して本体は約 8 cm しかない。
本体がようやく不透明になる高域で測っても結果は変わらなかった。

**到達時間差でも取れない。ただし理由が違う。** 回折は伝搬遅延を消さないので、
相互相関のほうが適した道具であり、実際に何かを見つけた——正面の話者は35窓中30窓で
**ぴったり遅延0**を出す。後ろの話者は −1 から −24 まで散らばり、山ができない。
これは遅延ではなく、**弱くなった直接音が反響に負けている**状態である。

正面の音源が確実に0になるということは、2つのカプセルがそこから等距離、つまり
**左右対称に並んでいる**ことを意味する。そして真後ろの音源も等距離なので、
やはり0になる。これは2素子アレイの front-back ambiguity であり、
調整の問題ではなく幾何の問題である。

アームがそれを決定的にする。アームが回転するのは**前後軸**——マイクが分解できない
まさにその軸である。マイクが分解できる左右は、アームが向けられない唯一の方向だ。

前後の両方を撮ればこの問題は回避できる。話している人は必ずどちらかの写真に写る。

## 質問の流れ

```
LIFELOG ──2回押し──▶ ARMED ──発話検出──▶ CAPTURING ──無音──▶ PROCESSING
   ▲                   │                                        │
   └────────────── タイムアウト ───────────────────────── 回答 ──┘
```

- **ジェスチャ**はアクセシビリティサービスが受け取る。サイドボタンはこれ以外の
  方法では観測できない（[運用上の注意](#運用上の注意)参照）。
- **ARMED** で振動し、`ASK / Speak now` を表示する。このボタンは物理的には電源キー
  なので、押すと画面が暗くなる。画面表示と振動がないと、端末が聞いているのかどうか
  利用者にはまったく分からない。8秒でタイムアウト。もう一度押せばキャンセル。
- **CAPTURING** は VAD が発話を検出した時点で始まり、押下の300ミリ秒前から、
  発話開始がそれより早ければその時点から切り出す。喋っている途中で押しても、
  既に口から出ている語を落とさない。
- **PROCESSING** は無音2秒で始まる。当初仕様の1.2秒ではない。実際の日本語音声に
  当てたところ、1.2秒では文が途中で切れた。

往復はおよそ8秒、うち4秒程度が文字起こし。

## 幻覚された無音の除去

静かな部屋を与えられた Whisper は学習データを返す。`Thank you.`、
`ご視聴ありがとうございました`、`Продолжение следует...`。ある一晩の1254
セグメント中、100件以上に "Thank you" が含まれ、午前3時台は丸ごと創作だった。

`no_speech_prob` は役に立たなかった。そのうちの1件に対し **0**、すなわち
「間違いなく発話である」と返してきた。両者を分けるのは、セグメント中のどれだけの
区間に Whisper が実際に語を置いたかである。

| | speech ratio | 言語判定の確信度 |
| --- | --- | --- |
| 就寝中 | 0.012 – 0.060 | `en` / 0.38 – 0.53 |
| 会話中 | 0.589 – 0.707 | `ja` / 0.99 – 1.00 |

一桁違うので、閾値は間の開けた場所、**0.15** に置いた。

書き込み時ではなく読み出し時に絞る。理由はもう1つ増えた。閾値を後から見直せる
ことに加えて、**幻覚された文字起こしは「索引」の欠陥であって「録音」の欠陥では
ない。** その裏にある音声は他のどの1分とも同じく実在する。

## 1日の流れと、それを決める者

端末は自分で動く。**7:00 に録音を開始し、23:00 に今日はここまでにするか尋ねる。**
答えがなければ10分ごとに聞き直す——**録音は動かしたまま。** この非対称が確認画面
の設計そのものである。答えられなかった質問がアーカイブを終わらせてはならない。
無回答の最もありそうな理由は「そこに誰もいなかった」だからだ。

**決定は0時ではなく翌朝まで有効。** 最初の版は「今日」で比較していたので、
23:04 に「止める」と答えても 0:00 に無効化され、0:09 に再開していた。気づいたのは
ログの中でであって、体感でではない。夜の回答は永続化もしている。ここでは再
インストールと再起動が頻繁で、どちらもなければ、意図的に与えた指示を黙って
覆してしまうからである。

同じ規則が、いまや**録音の作られ方を決めるすべて**に及んでいる。wake lock、
入力ソース、コーデック、記録フラグは以前は設定画面のフィールドで、起動のたびに
黙って既定値に戻っていた。録音の作られ方を決めるものは、それを設定する画面より
長く生きなければならない。

## 端末が表示するもの

出会う順に3つ。

- **ホーム**は5行のメニュー——話す・記録・待受・設定・Hermes——をホイールで送り、
  1行だけが点灯する。最初は小さな Android ボタンの列だったが、それはこの端末が
  最も苦手な操作、**240 dp の画面で 30 dp の的を叩くこと**を要求する。アプリは
  HOME アクティビティとしても登録してあるので、端末は純正ランチャーではなく
  ここへ戻る。
- **待受**は1分間操作がないと出る。**ただし給電中のみ。** ここに何を出そうと
  画面を点けたままにするので、バッテリー駆動でそれをやると1日分の録音が半日に
  なる。ホイールは画面を送るだけで、決して解除しない。抜けるのはサイドボタンの
  仕事である。
- **夜の確認画面**は Android 風ではなく R1 風にした。**探していなかった人の目に
  最も触れやすい画面**だからである。

3つとも、Activity でキーを横取りするのではなく Android のフォーカス機構に乗せて
いる。クリック可能なビューはフォーカス可能であり、`onKeyDown` が呼ばれる前に
D-pad を飲み込む——どの画面も最初の版は `onKeyDown` を上書きして、ホイールが
まったく効かなかった。

サイドボタンが二役を持つこと自体も罠である。フォーカス行を起動し、**同時に**
KeyService にも届く。KeyService は440ミリ秒待って「単押しだった」と判定し待受を
解除するので、メニューから待受を選ぶと待受が開いて即座に閉じた。現在はハンドラに
押下時刻を渡し、**その後に現れたものは解除しない**。

## 閲覧

`GET /` が単一ファイルの Web UI を返す。1日ぶんの音声と写真を時刻順に混ぜて並べ、
全期間検索が付き、もう1つのタブに同じ日のトラックが出る。ビルド手順も依存もない
——Worker がバックエンドの全部で、これがフロントエンドの全部である。ライブラリを
必要とするのは地図だけで、それも毎回ではなく初回表示時に読み込む（[地図](#地図)
を参照）。

認証は Cloudflare Access に依存している。ブラウザはページ遷移に bearer を付けられ
ないからである。その前提が何に支えられているかは[運用上の注意](#運用上の注意)を参照。

無音セグメントは省略せず、トグルの裏に隠す。**ページはバケットと一致できなければ
ならない。** 文字起こしのない録音も録音であり、それを黙って落とすページは
アーカイブを実際より小さく見せる。

## 運用上の注意

この端末について、忘れると必ず痛い目を見ることが5つある。

**サイドボタンはランチャーのものである。** CarrotOS は
`com.r1.launcher/.PowerService`（アクセシビリティサービス）で受け取っている。
ウィンドウフォーカスより手前で処理されるため、画面が消えていても効くし、
普通のアプリでは横取りできない。ランチャーはブロードキャストを出さない。
一方、キーフィルタリングを要求する2つ目のアクセシビリティサービスには
同じイベントが7ミリ秒遅れで届き、`false` を返せばランチャー側の動作はそのまま
維持される。`/system` の改変も root も不要。

**再インストールするとキー配信が黙って止まる。** サービスは再バインドされ
`connected` もログに出るが、`onKeyEvent` は二度と呼ばれない。ランチャー側は
正常に動き続けるので、見た目では気づけない。インストールのたびにサービスを
オフ／オンし直す必要がある。しかもその操作は `am force-stop` の**後**に行うこと。
アプリを停止すると有効リストから外れるため。

**入れたばかりのアプリは通信できない。** CarrotOS はネットワークポリシーに
`REJECT_ALL` を設定する。端末シェルからは18ミリ秒でホストに ping が通るのに、
アプリからは名前解決すらできず、`getActiveNetwork()` は null を返す。これは本当に
ネットワークがないときの戻り値と同じで、区別する手段がない。だからリンク状態は
`NetworkCallback` から取り、さらにアップローダは毎回 `ConnectivityManager` を
読み直す。`onLost` は「*ある*ネットワークが消えた」ときに発火するのであって、
端末がオフラインになったときではない。これを額面どおり受け取った結果、サーバに
39ミリ秒で ping が通る端末の上で325セグメントが何時間も滞留した。

**録音は可視 Activity からしか開始できない。** Android 14 ではマイクの
フォアグラウンドサービスをバックグラウンドから開始できないため、録音を再開したい
もの——起動時レシーバ、朝の自動開始、シェル——はすべて Activity 経由という遠回りを
する: `am start -n com.r1.audioprobe/.MainActivity --ez autostart true`。
Activity が既に起動しているときは `-f 0x20000000` が要る。付けないと `am start` は
タスクを前面に出して extras を捨て、**コマンドは成功したように見える。**

**Cloudflare Access の外側では何も検証していない。** Worker は
`Cf-Access-Jwt-Assertion` ヘッダを持つリクエストを、署名を検証せずに通す。これが
成り立つのは、Access が独自ドメインの**全パス**を覆っていて `workers.dev`
ルートが存在しない間だけである。今日は両方とも真だが、どちらもダッシュボードの
1回の編集で偽になる。端末の bearer トークンが第2要素だが、そのトークンは管理系
エンドポイントも開く。うち1つは R2 のオブジェクトを書き換える。**取り込みと管理は
資格情報を分けるべきで、分けていない。**

## 未実装

- **アーカイブの複製。** バイト列は端末が自分のディスクから読んだ内容を
  ハッシュし、R2 が不一致の書き込みを拒否するところまで来た——転送中の破損は
  捕まる。**消失は捕まらない。** バケット1つ、アカウント1つ、誤操作1回で全部が消える。
- **取り込みと管理で資格情報を分けること。** 上記参照。
- **文脈スライスが 23 MB。** 質問に渡す2分間は依然として WAV で、48 kHz ステレオ化に
  よって倍増した。Whisper が動き出す前に同期でアップロードするため、往復時間の
  最大要因になっている。Opus 化すべき。
- **再起動後のサイドボタン復帰。** 録音は復帰するが、アクセシビリティサービスが
  `enabled_accessibility_services` から外れ、手で戻すまで質問機能は死んだままになる。
  127.0.0.1:1337 の root シェルから書き戻せる。
- **TTS。** 回答は読むもので、読み上げない。
- **実測から決めたシェイクのしきい値。** 14 m/s² は推論であって計測ではなく、
  55分の歩行で1回誤爆した——だからジェスチャは画面が点いているときだけ効くように
  した。症状は消えたが問いには答えていない。ピーク強度をログに出すようにしたので、
  2回と3回の境界を決めたのと同じやり方で決められる。
  [`BACKLOG.md`](BACKLOG.md#unverified-the-shake-threshold) を参照。
- **会話モード。** 「まだない」ではなく「設計して寝かせた」もの。計測側だけは
  実装済みで、`GET /v1/talk` が1日ごとの発話を含む分数を返す——「今日すでに
  話した日は起動しない」を成立させるには、しきい値を実際の日々から取る必要が
  あるため。[`BACKLOG.md`](BACKLOG.md) を参照。

## 実測したハードウェア仕様

画面サイズ、キーコード、録音フォーマット、消費電流、バッテリー曲線の各数値は
[`audioprobe/FINDINGS.md`](audioprobe/FINDINGS.md) を参照。
