# 設計（実装済みの姿）

現在のシステムが何をしているか、そして当初の仕様書からどこが変わったか。
以下の相違はすべて、端末またはデータ上の実測によって強制されたものである。
判断の根拠となった数値を残してあるので、事実が変われば議論し直せる。

English: [`DESIGN.md`](DESIGN.md)

## これは何か

周囲で交わされている会話について質問できる Rabbit R1。

サイドボタンを2回押して質問すると、直前2分間を文脈として渡された自前ホストの
Hermes エージェントから答えが返る。それ以外のとき、端末は沈黙している。
ディスクには何も書かず、何も送信しない。

**常時ライフログではない。** 最初は常時ライフログとして作り、18時間動かし、
その上で意図的に録音を外した。理由は[ライフログを外した理由](#ライフログを外した理由)に記す。

## 全体像

```
  サイドボタン ──▶ KeyService (アクセシビリティ)
                       │ 2回押し
                       ▼
  マイク ──────▶ RecorderService ──▶ RingBuffer (150秒・メモリのみ)
                       │                   │
                       │ VAD               │ 質問時: 発話 + 直前2分
                       ▼                   ▼
                 QueryController ──▶ PUT /v1/segments/{id}?sync=1
                                             │
                                    Cloudflare Access
                                             ▼
                                     Worker ──▶ R2 (音声・1日)
                                             └─▶ Whisper ──▶ D1 (文字起こし・1日)
                                                              │
  ChatActivity ◀── 文字起こし                                  │
       │                                                      │
       └──▶ Hermes gateway ──▶ agent ──MCP: lifelog_recent────┘
```

この図で注目すべき点が2つある。

**文脈はエージェントが自分で取りに行く。** 文字起こしと文脈ブロブを一緒に運ぶ
専用の質問 API は存在しない。R1 は普通のプロンプトを投げるだけで、`lifelog_recent`
を呼ぶかどうかはエージェントが決める。これで当初設計からエンドポイントが1本まるごと
消え、「さっきの話」が特別扱いなしに機能するようになった。

**既定ではリングバッファが音声の唯一の置き場である。** メモリ上、150秒、
常に上書きされ続ける。

## 質問の流れ

```
LIFELOG ──2回押し──▶ ARMED ──発話検出──▶ CAPTURING ──無音──▶ PROCESSING
   ▲                   │                                        │
   └────────────── タイムアウト ───────────────────────── 回答 ──┘
```

- **ジェスチャ**はアクセシビリティサービスが受け取る。サイドボタンはこれ以外の
  方法では観測できない（[運用上の注意](#運用上の注意)参照）。
- **ARMED** で振動し、`ASK / Speak now` を表示する。このボタンは物理的には電源キー
  なので、押すと画面が暗くなる。画面表示と振動がないと、端末が聞いているのかどうか
  利用者にはまったく分からない。8秒でタイムアウト。もう一度押せばキャンセル。
- **CAPTURING** は VAD が発話を検出した時点で始まり、押下の300ミリ秒前から、
  発話開始がそれより早ければその時点から切り出す。喋っている途中で押しても、
  既に口から出ている語を落とさない。
- **PROCESSING** は無音2秒で始まる。当初仕様の1.2秒ではない。実際の日本語音声に
  当てたところ、1.2秒では文が途中で切れた。
- その後、質問の直前2分を送り、続いて質問本体を `sync=1` で送り、
  文字起こしをチャットクライアントに渡す。

往復はおよそ8秒、うち4秒程度が文字起こし。

## 保持期間

1日。互いを知らない2箇所で個別に強制する。

| 対象 | 場所 | 方法 |
| --- | --- | --- |
| 音声 | R2 | バケットのライフサイクルルール `expire-audio-1d`（`audio/` 配下） |
| 文字起こし | D1 | 毎時 cron が `started_epoch` を掃く |

いずれも**セグメント自身の開始時刻**を基準とする。到着時刻ではない。
Wi-Fi が見つかるまで送信を遅らせたセグメントも、予定どおり期限切れになる。

## ライフログを外した理由

常時版は動いた。バッテリー駆動で18時間無人稼働し、フレーム落ちはなく、
エージェントは任意の1時間を要約できた。外したのは、質問機能にそれが必要なかった
から（要求時に2分の文脈を取れば同じ仕事ができる）、そして出力の大半がノイズ
だったからである。

- **大半が作り話だった。** 静かな部屋を与えられた Whisper は学習データを返す。
  `Thank you.`、`ご視聴ありがとうございました`、`Продолжение следует...`、
  `Hello everyone, welcome back to my channel`。1254セグメント中、100件以上に
  "Thank you" が含まれていた。午前3時台は丸ごと幻覚だった。
- **月約22ドルかかり、1日2.4GB を送信していた。** Wi-Fi なら問題ないが、
  SIM では論外。

**ただしこのコストは VAD を通していないパイプラインの数字であり、
「ライフログという方式そのもの」への反証には使えない。** VAD は実装済みだが、
配線されていたのは質問経路（`query.tick(...)`）だけで、ライフログは無音を含む
全フレームを書き、送っていた。上記の幻覚もこれが原因である。VAD が書き込みを
ゲートしていれば、完全な無音のセグメントは1つも上がらなかった。
実際にどれだけ削れたかは次節に示す。

幻覚の問題はサーバ側で除去済みなので、ライフログを戻すことはできる。
`Lifelog: ON` で復活する。ただし VAD を書き込み経路の手前に入れるまでは、
ゲートなしのままである。

## VAD を手前に入れた場合の効果

直近24時間のうち、Whisper 自身のセグメント時刻を持つ589セグメントからの実測
（残りは時刻を返さないフォールバックモデルに落ちている）。

| 時刻（JST） | 録音時間に占める有声の割合 |
| --- | --- |
| 03時（就寝中・1セグメントのみ） | 3.2 % |
| 12 – 19時（勤務中） | 4.4 – 19.9 %、平均 約11 % |
| 20 – 23時（帰宅後・会話あり） | 10.2 – 52.8 %、平均 約31 % |

就寝時間を含む1日全体に外挿すると、**録音のうち発話は概ね11%程度**。
これだけを文字起こしすれば月22ドルではなく約2.4ドル、送信量は1日2.4GBではなく
約260MBになる。

ただしこれをそのまま削減幅と呼べない理由が2つある。

- `speech_ratio` は Whisper の語カバー率であって、エネルギー VAD の判定ではない。
  RMS しきい値はエアコン・車の音・キーボードも通す。余裕は十分にある
  （背景 RMS 352 に対し発話 2440–3536）が、うるさい部屋では11%より多く通る。
- ハングオーバー2秒とプリロールが発話ごとに付くため、短い発言が散らばると
  有声秒数よりずっと高くつく。

現実的には**削減率70–85%、すなわち月3–7ドル**というところ。22ドルでもなく
2.4ドルでもない。いずれにせよ要点は変わらない。発話でゲートすればライフログの
コストは一桁変わり、それを試す前にオフにしたということである。

## 幻覚された無音の除去

`no_speech_prob` はここでは役に立たなかった。午前3時の "Thank you. Thank you."
というセグメントに対し、**0**、すなわち「間違いなく発話である」と返してきた。

両者を分けるのは、セグメント中のどれだけの区間に Whisper が実際に語を置いたか
である。

| | speech ratio | 言語判定の確信度 |
| --- | --- | --- |
| 就寝中 | 0.012 – 0.060 | `en` / 0.38 – 0.53 |
| 会話中 | 0.589 – 0.707 | `ja` / 0.99 – 1.00 |

一桁違うので、閾値は間の開けた場所、**0.15** に置いた。

これらの指標は保存しておき、書き込み時に捨てるのではなく読み出し時に絞り込む。
文字起こしは分単位課金であり、取り込み時に閾値を焼き込んでしまうと、
後から見直すには全コーパスを金を払って再実行するしかなくなる。

## コスト

| | |
| --- | --- |
| Workers AI Whisper | 音声1分あたり $0.00051 |
| R2 | $0.015/GB・月、下り無料、10GB まで無料枠 |
| 1回あたり約2.5分の音声を伴う質問 | 月1ドル未満 |
| 置き換えた常時ライフログ（VAD なし） | 月約22ドル |
| 同じライフログに VAD を入れた場合 | 月3–7ドル（推定） |

## 運用上の注意

この端末について、忘れると必ず痛い目を見ることが3つある。

**サイドボタンはランチャーのものである。** CarrotOS は
`com.r1.launcher/.PowerService`（アクセシビリティサービス）で受け取っている。
ウィンドウフォーカスより手前で処理されるため、画面が消えていても効くし、
普通のアプリでは横取りできない。ランチャーはブロードキャストを出さない。
一方、キーフィルタリングを要求する2つ目のアクセシビリティサービスには
同じイベントが7ミリ秒遅れで届き、`false` を返せばランチャー側の動作はそのまま
維持される。`/system` の改変も root も不要。

**再インストールするとキー配信が黙って止まる。** サービスは再バインドされ
`connected` もログに出るが、`onKeyEvent` は二度と呼ばれない。ランチャー側は
正常に動き続けるので、見た目では気づけない。インストールのたびにサービスを
オフ／オンし直す必要がある。しかもその操作は `am force-stop` の**後**に行うこと。
アプリを停止すると有効リストから外れるため。

**入れたばかりのアプリは通信できない。** CarrotOS はネットワークポリシーに
`REJECT_ALL` を設定する。端末シェルからは18ミリ秒でホストに ping が通るのに、
アプリからは名前解決すらできず、`getActiveNetwork()` は null を返す。
これは本当にネットワークがないときの戻り値と同じで、区別する手段がない。

3つ目が、リンク状態を `getActiveNetwork()` ではなく `NetworkCallback` から
取っている理由であり、さらにアップローダが毎回 `ConnectivityManager` を
読み直している理由でもある。`onLost` は「*ある*ネットワークが消えた」ときに
発火するのであって、端末がオフラインになったときではない。これを額面どおり
受け取った結果、サーバに39ミリ秒で ping が通る端末の上で325セグメントが
何時間も滞留した。

## 未実装

- **ライフログ書き込み経路の VAD ゲート** — ゲート自体は存在し質問経路を
  動かしているが、録音をゲートしたことは一度もない。`Lifelog: ON` に戻すなら、
  先にこれを配線すること。試算は上記のとおり。
- **Opus** — 実装済み・ビルドも通るが、トグルはオフ。ライフログがなくなった今、
  未検証のコーデック経路を通すリスクに見合う転送量がない。
- **TTS** — 回答は読むもので、読み上げない。
- **ウェイクワード** — 起動手段はジェスチャのみ。
- **72時間耐久試験** — 18時間は問題なく通過。バッテリーが約20時間なので、
  これ以上は電源接続が要る。

## 実測したハードウェア仕様

画面サイズ、キーコード、録音フォーマット、消費電流、バッテリー曲線の各数値は
[`audioprobe/FINDINGS.md`](audioprobe/FINDINGS.md) を参照。
